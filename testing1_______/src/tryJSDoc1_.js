/**
 * a fun !
 * @param {number} x1
 * @param {number} x2
 * @returns {number}
 */
function fun1(x1,x2) {
    return x1 * x2;
}

const xxx = fun1("abc",null);
console.log(xxx);

/**
 * A song
 * @typedef {Object} Song
 * @property {string} title - The title
 * @property {number} year - The year
 */

/** @type {Array<Song>} some number value */
var songs;
songs.push({
    title:"",
    year:1
});
